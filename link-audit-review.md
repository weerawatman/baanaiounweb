# ตรวจสอบลิงก์/ปุ่มทั้งเว็บ — เอกสารคุยกับเจ้าของเว็บ

**วันที่:** 2026-07-03
**ที่มา:** ผู้ใช้ทดลองกดปุ่ม/ลิงก์ทั่วเว็บ พบว่าบางปุ่มกดแล้วไม่เกิดอะไรขึ้น (ค้างอยู่หน้าเดิม)
บางปุ่มกดแล้วไปหน้าที่ไม่คาดคิด จึงตรวจสอบโค้ดทุกหน้า (ทุก `<Link>`/ปุ่ม) เทียบกับ
route จริงที่มีในระบบและ redirect ทั้งหมดใน `next.config.ts`

**สรุปตัวเลข:** พบลิงก์ที่กดแล้วไม่ทำงานจริง **4 จุด**, หน้าที่มีโค้ดอยู่แต่กดเข้าไม่ถึงเลย
**5 หน้า**, ลิงก์ที่ไปหน้าไม่ตรงชื่อปุ่ม **1 จุด**, ลิงก์ที่ใช้งานได้แต่อ้อมผ่าน redirect
โดยไม่จำเป็น **4 จุด**

**อัปเดต 2026-07-03 (รอบ 2):** แก้ไข **หมวด D** (ลิงก์อ้อม) และ **หมวด B** (5
หน้าที่เข้าไม่ถึง) เรียบร้อยแล้ว ตาม 2 เรื่องหลักที่ตกลงกันไว้ก่อน — รายละเอียด
การแก้ไขอยู่ในแต่ละหมวดด้านล่าง หมวด A และ C ยังรอการตัดสินใจจากเจ้าของเว็บ

---

## หมวด A: ลิงก์ที่กดแล้วไม่เกิดอะไรขึ้นจริง (บั๊ก ควรแก้)

| ปุ่ม/ข้อความ | อยู่หน้าไหน | ปัญหา | ตัวเลือกแก้ |
|---|---|---|---|
| "นโยบายความเป็นส่วนตัว" (Privacy Policy) ที่ Footer | **ทุกหน้า** (`Footer.tsx:124`) | ลิงก์เป็น `href="#"` — ในโค้ดมีคอมเมนต์ยอมรับเองว่า "ยังไม่สร้างหน้าจริง" | (1) เขียนหน้า Privacy Policy จริง — แนะนำก่อน launch เพราะเว็บมีฟอร์มเก็บข้อมูลลูกค้า (2) ซ่อนลิงก์นี้ออกจาก Footer ชั่วคราวจนกว่าจะมีหน้าจริง (3) ลิงก์ไปหน้า Contact แทนไปพลางก่อน |
| ปุ่มหลัก "ค้นหาบ้าน/ปรึกษาสินเชื่อ" | `/find-property` (`content/find-property.ts:76`) | ลิงก์ไป `href="#form"` แต่ไม่มี element ไหนในหน้ามี `id="form"` เลย — กดแล้วไม่มีอะไรเกิดขึ้น (หน้าอื่นๆ ที่คล้ายกัน เช่น Co-Agent, Agent Course ใช้วิธีเปิดฟอร์มแบบถูกต้อง แต่หน้านี้ตั้งค่าผิด) | **(1) แก้ให้เปิดฟอร์มถูกต้องเหมือนหน้าอื่น — เป็นบั๊กจริง ควรแก้ก่อน launch** (2) ลบปุ่มนี้ (3) เปลี่ยนไปลิงก์หน้าอื่น |
| ปุ่มรอง "ดูทรัพย์ที่มี" | `/find-property` (`content/find-property.ts:80`) | ลิงก์ไป `href="#listings"` แต่หน้านี้ไม่มี section รายการทรัพย์เลย ไม่มี `id="listings"` | (1) เพิ่ม section แสดงทรัพย์ในหน้านี้ (2) เปลี่ยนปุ่มให้ลิงก์ไป `/properties` แทน (3) ลบปุ่มนี้ออก |
| ปุ่มหลัก "ฝากขาย-ปล่อยเช่า" | `/list-property` (`content/list-property.ts:76`) | ลิงก์ไป `href="#form"` เหมือนปัญหาข้างต้น — กดแล้วฟอร์มไม่เปิด | **(1) แก้ให้เปิดฟอร์มถูกต้อง — บั๊กจริง ควรแก้ก่อน launch** (2) ลบปุ่มนี้ (3) เปลี่ยนไปลิงก์หน้าอื่น |

---

## หมวด B: หน้าที่มีโค้ดอยู่จริง แต่กดเข้าไม่ถึงเลย ✅ แก้ไขแล้ว

ใน `next.config.ts` มีการตั้ง redirect (301 ถาวร) ซึ่งจะทำงาน**ก่อน**ระบบ route
เสมอ ทำให้หน้าด้านล่างนี้ **ไม่มีทางแสดงผลได้เลย** แม้จะพิมพ์ URL ตรงๆ ก็ตาม —
เป็นโค้ดที่ตายแล้ว (dead code) ทั้งหมด

**การแก้ไข:** ลบไฟล์หน้าเหล่านี้ทิ้งตามตัวเลือก (1) — สอดคล้องกับเมนูใหม่ที่ปรับ
ไปแล้ว (เมนูลิงก์ตรงไป `/find-property` / `/list-property` / `/agent-course`
อยู่แล้ว ไม่มีที่ไหนอ้างอิงหน้าเดิมพวกนี้อีก) `redirects()` ใน `next.config.ts`
**ยังคงไว้เหมือนเดิมทุกจุด** เพื่อรองรับลิงก์เก่าที่อาจถูก bookmark หรือ index ไว้
โดย Google — ทดสอบแล้วว่าทุก path ยัง redirect ไปที่ถูกต้อง:

| Path เดิม | Redirect ไปที่ | สถานะ |
|---|---|---|
| `/buy` | `/find-property` | ✅ ลบไฟล์หน้าแล้ว, redirect ทำงานปกติ |
| `/rent` | `/find-property` | ✅ ลบไฟล์หน้าแล้ว, redirect ทำงานปกติ |
| `/land` | `/find-property` | ✅ ลบไฟล์หน้าแล้ว, redirect ทำงานปกติ |
| `/owners` | `/list-property` | ✅ ลบไฟล์หน้าแล้ว, redirect ทำงานปกติ |
| `/academy` | `/agent-course` | ✅ ลบไฟล์หน้าแล้ว, redirect ทำงานปกติ |

ไฟล์ที่ลบ: `src/app/(public)/{buy,rent,land,owners,academy}/` ทั้งโฟลเดอร์ และ
ไฟล์เนื้อหาที่ใช้เฉพาะหน้าพวกนี้ `src/content/{buy,rent,land,owners,academy}.ts`
(ตรวจสอบแล้วว่าไม่มีไฟล์อื่นในระบบ import ไฟล์เหล่านี้)

หมายเหตุ: `sitemap.ts` ที่มีอยู่แล้วก็ไม่รวม 5 หน้านี้ไว้ (มีคอมเมนต์ในโค้ดว่า "no
redirected paths") แสดงว่าทีมเดิมก็รู้อยู่แล้วว่าหน้าพวกนี้ใช้งานจริงไม่ได้

---

## หมวด C: ลิงก์ที่ไปหน้าไม่ตรงกับชื่อปุ่ม — กลายเป็นไม่เกี่ยวแล้วหลังลบหมวด B

เดิมพบว่าปุ่ม "ดูผลงานปิดดีลของเรา" ในหน้า `/owners` ลิงก์ไป `/blog` (ไม่ตรงกับ
ชื่อปุ่ม) แต่เนื่องจากหน้า `/owners` **ไม่มีทางถูกผู้ใช้จริงเห็นอยู่แล้ว** (โดน
redirect ไป `/list-property` เสมอ ตามหมวด B) และตอนนี้ไฟล์หน้านี้ถูกลบไปแล้ว
ปุ่มนี้จึงไม่มีอยู่ในระบบอีกต่อไป — **ไม่ต้องตัดสินใจอะไรเพิ่มแล้วในเรื่องนี้**

**แนวคิดเผื่ออนาคต (ไม่เร่งด่วน):** ถ้าอยากให้เจ้าของทรัพย์เห็น "ผลงานปิดดีล"
จริงๆ ตอนกดฝากขาย/เช่า อาจพิจารณาเพิ่ม section แบบนี้ในหน้า `/list-property`
(หน้าจริงที่ผู้ใช้เห็น) แทน — เป็นเรื่องเนื้อหาใหม่ ไม่ใช่บั๊ก ไว้คุยกันทีหลังได้

---

## หมวด D: ลิงก์อ้อม (ใช้งานได้ แต่ผ่าน redirect โดยไม่จำเป็น) ✅ แก้ไขแล้ว

จุดพวกนี้**ไม่ได้พังจริง** — ผู้ใช้กดแล้วถึงปลายทางถูกต้อง แต่เสียเวลาอ้อมผ่าน
301 redirect โดยไม่จำเป็น แก้ให้ชี้ตรงไปหน้าจริงเลยแล้ว (สำคัญกว่าเดิมด้วย
เพราะหลังลบหมวด B แล้ว Breadcrumb ที่หน้ารายละเอียดทรัพย์ **ต้อง**ชี้ไปหน้าที่
มีอยู่จริงโดยตรง ตรวจสอบแล้วว่า render ถูกต้อง):

| จุดที่เคยลิงก์อ้อม | ไฟล์ | แก้เป็น | สถานะ |
|---|---|---|---|
| การ์ด pain-point ในหน้าแรก | `content/homepage.ts:118` | `/owners` → `/list-property` | ✅ |
| การ์ด pain-point ในหน้าแรก | `content/homepage.ts:124` | `/buy` → `/find-property` | ✅ |
| Breadcrumb ในหน้ารายละเอียดทรัพย์ (ทุกประเภท: ขาย/เช่า/ที่ดิน) | `property/[slug]/PropertyDetailClient.tsx:32-34` | `/buy`, `/rent`, `/land` → `/find-property` ทั้งหมด | ✅ ทดสอบแล้วว่า breadcrumb ขึ้น `/find-property` ถูกต้อง |

---

## ไว้ดูทีหลัง (ไม่ใช่ปัญหาเร่งด่วน — กันลืมไว้เฉยๆ)

รายการนี้**ไม่เกี่ยวกับการเชื่อมโยงระหว่างหน้า** โดยตรง เป็นเรื่องเล็กๆ ที่เจอ
ระหว่างตรวจโค้ด บันทึกไว้เผื่อกลับมาดูภายหลัง:

- **URL ไลน์ไม่ตรงกัน:** หน้าบทความ (`BlogPostClient.tsx`) ใช้ลิงก์ LINE
  `line.me/ti/p/~@baanaioun` ซึ่งต่างจากที่ใช้ทั่วเว็บ (`@baan-ai-oun` ใน
  `SITE_CONFIG`) — ควรเช็คว่า handle ไหนถูกต้อง
- Footer มีลิงก์ YouTube ที่ยังเป็น URL ตัวอย่าง (มีคอมเมนต์ `TODO: อัปเดต URL
  จริง` ในโค้ด)
- หน้า `/properties` ตอนไม่พบผลลัพธ์ ปุ่ม "ติดต่อพิมโดยตรง" ใช้ `<a>` ธรรมดาแทน
  ที่จะใช้ระบบนำทางของเว็บ ทำให้หน้ารีโหลดใหม่ทั้งหน้าโดยไม่จำเป็น (ไม่ใช่บั๊ก
  แค่ทำงานช้ากว่าที่ควร)
- คอมโพเนนต์ `src/components/home/SearchBar.tsx` ไม่ได้ถูกเรียกใช้ที่ไหนในเว็บ
  เลย (dead code) — ถ้าไม่มีแผนใช้ในอนาคตอาจลบทิ้งได้
- ข้อมูลใน `content/homepage.ts` ส่วน `HOMEPAGE_SERVICES`/`HOMEPAGE_PAIN_POINTS`
  ก็ไม่ได้ถูกเรียกใช้ในหน้าแรกเช่นกัน (dead code)

---

## คำถามเปิดสำหรับเจ้าของเว็บ (เหลือแค่หมวด A)

หมวด B, C, D จัดการเรียบร้อยแล้ว เหลือแค่หมวด A ที่ต้องตัดสินใจ:

1. **หมวด A (บั๊กจริง):** ปุ่ม "ค้นหาบ้าน/ปรึกษาสินเชื่อ" กับ "ฝากขาย-ปล่อยเช่า"
   ที่ควรเปิดฟอร์มแต่ไม่เปิด — อันนี้กระทบ Lead Generation โดยตรง ควรแก้เป็น
   อันดับแรกไหม?
2. **หมวด A:** ปุ่ม Privacy Policy ที่ Footer (`href="#"`) — เขียนหน้าจริง /
   ซ่อนไปก่อน / ลิงก์ไป Contact ชั่วคราว แบบไหนดี?
3. **หมวด A:** ปุ่มรอง "ดูทรัพย์ที่มี" ในหน้า Find Property — เพิ่ม section
   แสดงทรัพย์ในหน้านั้นเลย หรือลิงก์ไป `/properties` แทนดี?
